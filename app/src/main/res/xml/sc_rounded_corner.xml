<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/alert_default_press">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/alert_default_press" />
            <corners android:radius="6dp" />
        </shape>
    </item>

    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <gradient
                android:angle="90"
                android:endColor="@color/alert_default"
                android:startColor="@color/alert_default"
                android:type="linear" />
            <corners android:radius="6dp" />
        </shape>
    </item>
</ripple>